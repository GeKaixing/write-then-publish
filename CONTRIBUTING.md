# 参与贡献

感谢你愿意帮助改进「文象」。这是一个纯前端项目，参与方式很直接。

## 如何开始

1. Fork 本仓库并克隆到本地。
2. 项目是纯前端静态页面，无需安装依赖。
3. 运行 `npm start`，在 `http://127.0.0.1:5173/` 验证修改。
4. 提交前先跑一遍本地验证：启动服务、打开页面、确认图文卡片与长文模式都能正常渲染和下载。

## 改动范围

- 网页端修改 `index.html`、`src/app.js`、`src/styles.css`。
- 同步修改 Obsidian 插件或 Chrome 扩展后，运行 `npm run sync` 保持各端一致。
- 版本号统一由 `package.json` 管理，升级时运行 `npm run sync`。

## 提交规范

- 提交信息使用简短中文或英文描述，例如 `fix: 修复导出长图空白`。
- 只提交与本次改动相关的文件，不要把本地生成的目录（如 `out/`、`.hermes/`）推入仓库。
- 涉及用户可见行为的改动，请在提交说明中写明验证方式。

## 提问与讨论

遇到问题可以提交 Issue，说明复现步骤、浏览器版本和 `npm start` 后的访问地址。
