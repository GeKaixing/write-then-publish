# 安全说明

「文象」是纯前端本地工具。内容、图片、历史记录和设置默认只保存在浏览器本地，不上传任何服务器。

## 报告漏洞

如果发现安全问题（例如图片跨源导致 Canvas 数据泄露、插件越权读取仓库文件），请不要在 Issue 中公开细节，直接通过 GitHub 私有联系方式提交，或给维护者发邮件说明问题与最小复现步骤。

## 已知边界

- 必须通过 `http://127.0.0.1:5173/` 使用，不要在不受信任的公共网页中嵌入本项目的 HTML 与脚本。
- Obsidian 插件和 Chrome 扩展只应授予其需要的目录与站点权限，不要修改 manifest 随意扩大权限。
